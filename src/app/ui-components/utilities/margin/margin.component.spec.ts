import { TestBed } from '@angular/core/testing';
import { MarginComponent } from './margin.component';

describe('Component: Margin', () => {
  it('should create an instance', () => {
    let component = new MarginComponent();
    expect(component).toBeTruthy();
  });
});