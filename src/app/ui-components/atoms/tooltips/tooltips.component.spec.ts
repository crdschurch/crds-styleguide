import { TooltipsComponent } from './tooltips.component';

describe('Component: Tooltips', () => {
  it('should create an instance', () => {
    let component = new TooltipsComponent();
    expect(component).toBeTruthy();
  });
});
